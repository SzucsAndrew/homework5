import { RandomGeneratorHelper } from './random-generator-helper';

describe('RandomGeneratorHelper', () => {
  it('should create an instance', () => {
    expect(new RandomGeneratorHelper()).toBeTruthy();
  });
});
